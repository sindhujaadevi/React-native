import React, { Component } from 'react'
import { View, Text } from 'react-native'

// 1. Creating component in a file and exporting
export default class MyComponent extends Component {
  render() {
    return (
        <Text>Hello Employee</Text>
    )
  }
}
